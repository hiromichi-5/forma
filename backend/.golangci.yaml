version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - containedctx
    - contextcheck
    - errorlint
    - gocritic
    - gosec
    - modernize
    - rowserrcheck
    - sqlclosecheck
    - unconvert
    - unparam

  exclusions:
    rules:
      # sqlc生成コード
      - path: \.sql\.go$
        linters: ["*"]
      # oapi-codegen生成コード
      - path: gen\.go$
        linters: ["*"]
      # e2eテスト: テストヘルパーの都合でlintが通らないため除外する
      - path: e2e/
        linters: [bodyclose, errcheck, unparam, gosec]
      # テストユーティリティ: テストヘルパーの都合でlintが通らないため除外する
      - path: internal/testutil/
        linters: [errcheck]

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
    - swaggo
  exclusions:
    rules:
      # sqlc生成コード
      - path: \.sql\.go$
        linters: ["*"]
      # oapi-codegen生成コード
      - path: gen\.go$
        linters: ["*"]
output:
  formats:
    json:
      path: ./linters-report.json
