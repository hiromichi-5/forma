# 基本原則
- 常に日本語でコミュニケーションを行ってください。すべてのコミットメッセージ、コメント、エラーメッセージ、ユーザーとのやり取りは日本語で行ってください。
- ファイルの作成、編集、削除を行う前に、必ず以下を報告し、明示的なユーザー承認を得てください。
  - 対象ファイルのリスト
  - 実行する変更の詳細説明
  - 影響範囲の説明
- ただし、.mdファイルを作成することの承認は不要です。
- 不明な点がある場合は常に質問し、推測で進めてはなりません。

# リポジトリガイド
- docs/*を確認してください。
- フロントエンドはtypescript/react/vite、バックエンドはgolang/ginです。
- makeコマンドを活用できる場合は積極的に活用してください。

# コーディングガイドライン
- 単に動作させるための変更をせず全体の見通しの良いコードベースを維持することを意識して修正してください。
- コメントは最小限にし、特に複雑なロジックでは*何をしているか*ではなく*なぜそうしているか*に焦点を当ててください。明確さのために必要な場合、またはユーザーから求められた場合にのみ、高付加価値のコメントを追加してください。不要なコメントでコードベースを散らかさないでください。コメントは原則として**WHY**を説明してください。コメントによる**WHAT**の説明は、真に必要な場合に限ってください。
- コマンドで自動的に作成・編集するべきものを手動で行わないでください。

## エラーハンドリング
- すべてのエラーをチェックし、適切に処理してください。
- エラーの重大度に応じて適切なロギングをしてください。
- 安易なフォールバックをしないでください。適切なエラーを返してください。

## テスト
- 新機能にはテストを書いてください。
- コードやテストを変更した後はテストを実行してください。

### Backendに特化したコーディングルール
- コードを修正した後は`go fmt ./...`, `go vet ./...`を実行してください。

### Frontendに特化したコーディングルール
- **`any`の禁止** いかなる理由があっても`any`を使用してはなりません。
- **型アサーションの禁止** どうしても型アサーションを使用する必要がある場合は、明確な理由をコメントとして記述してください。
- **`interface`の禁止** 型定義に`interface`を使用してはなりません。`type`を使用してください。

#### useEffect ポリシー
useEffect は **外部世界との同期** のためにのみ使用しなければなりません。  
例：API 呼び出し、WebSocket 接続、ブラウザ API、外部ストアの購読、タイマーなど。  
それ以外のケースでは、使用してはいけません。

###### アンチパターン
- props や派生した値をローカル state にコピーすること
- フラグの変更に反応してロジックを実行すること
- ユーザー操作をイベントハンドラではなく effect 内で処理すること
- 派生状態やバリデーション状態を effect 内で更新すること
- 空の依存配列による一度きりの初期化（代わりに useMemo を使用する）

###### 原則
1. props や state から導出できる値は、レンダー中に計算する。
2. ユーザー操作は effect ではなくイベントハンドラで処理する。
3. effect は外部システムに触れる「本当の副作用」にのみ限定する。
